# raspberry-pi-neomatrix
To get token:
  Top right corner, select "Settings"
  Select "Developer Settings" on the left menu
  Select "Personal Access Token" on the left menu
Use the token for password to Commit for example
